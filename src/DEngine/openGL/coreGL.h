#include <DEngine/core.h>

namespace DEngine {

    namespace Core {
        //DirectX11 related
        int InitGL(DE &engine);
        int CleanGL(DE &engine);
        void RenderGL(DE &engine);
    }
}