#pragma once

#include <DEngine/graphics.h>

namespace DEngine
{
  class DrawableObject
  {
  private:
    /* data */
  public:
    DrawableObject(/* args */);
    ~DrawableObject();

    void Draw();
  };

}